---
description: Fingerprint every anchor; --reset takes a new baseline, --reset --at REV takes it from git
argument-hint: [--reset [--at REV]]
---
!`python3 "${CLAUDE_PLUGIN_ROOT}/mangsang.py" observe $ARGUMENTS --target .`

Show the output above to the user as is. Do not interpret or act on it unless asked.
