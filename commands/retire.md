---
description: Drop a relation, keeping it and the reason in retired/
argument-hint: <id> --why WHY
---
!`python3 "${CLAUDE_PLUGIN_ROOT}/mangsang.py" retire $ARGUMENTS --target .`

Show the output above to the user as is. Do not interpret or act on it unless asked.
