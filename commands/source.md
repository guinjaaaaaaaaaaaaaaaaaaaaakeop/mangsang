---
description: Keep what was said or written, verbatim, as the anchor source:ID (either side of a conversation); list shows what each source grounds
argument-hint: add ID --file PATH|- --speaker WHO [--locator WHERE] [--replies-to ID] | list
---
!`python3 "${CLAUDE_PLUGIN_ROOT}/mangsang.py" source $ARGUMENTS --target .`

Show the output above to the user as is. Do not interpret or act on it unless asked.
