---
description: A human re-read stale relations and they still hold; `seen` becomes what the tree has now
argument-hint: <id>... --by NAME | --delegated WHY
---
!`python3 "${CLAUDE_PLUGIN_ROOT}/mangsang.py" reconfirm $ARGUMENTS --target .`

Show the output above to the user as is. Do not interpret or act on it unless asked.
