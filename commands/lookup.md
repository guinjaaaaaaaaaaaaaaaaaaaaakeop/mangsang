---
description: Before touching a file: the confirmed relations standing on it and which ones the edit would make stale — read-only
argument-hint: <path>
---
!`python3 "${CLAUDE_PLUGIN_ROOT}/mangsang.py" lookup $ARGUMENTS --target .`

Show the output above to the user as is. Do not interpret or act on it unless asked.
