---
description: Ask the net's invariants (coverage/projection/resolved) — its health, not its questions
argument-hint: [--findings]
---
!`python3 "${CLAUDE_PLUGIN_ROOT}/mangsang.py" check $ARGUMENTS --target .`

Show the output above to the user as is. Do not interpret or act on it unless asked.
